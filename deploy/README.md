# Скрипты безопасного деплоя

Набор скриптов для безопасного развертывания новой версии системы лояльности с возможностью отката.

## 📋 Содержание

- `safe_deploy.sh` - Создание бэкапа и подготовка к деплою
- `deploy_new_version.sh` - Развертывание новой версии после бэкапа
- `rollback.sh` - Откат на предыдущую версию

## 🚀 Процесс деплоя

### Шаг 1: Создание бэкапа

```bash
cd /root/home
git clone <repository> momsclub  # или обновите код другим способом
cd momsclub/deploy
chmod +x *.sh
sudo ./safe_deploy.sh 04112025
```

Где `04112025` - дата бэкапа (04 ноября 2025). Если не указать, будет использована текущая дата.

**Что делает скрипт:**
- ✅ Создает бэкап текущей версии в `/root/home/momsclub04112025`
- ✅ Копирует все файлы проекта (кроме venv, логов, кэша)
- ✅ Сохраняет базу данных `momsclub.db`
- ✅ Сохраняет файл `.env`
- ✅ Останавливает текущий сервис

### Шаг 2: Загрузка новой версии

Загрузите новую версию кода в `/root/home/momsclub`:
- Через git pull
- Через копирование файлов
- Через любой другой способ

### Шаг 3: Развертывание

```bash
cd /root/home/momsclub/deploy
sudo ./deploy_new_version.sh
```

**Что делает скрипт:**
- ✅ Обновляет зависимости из `requirements.txt`
- ✅ Запускает миграцию `database/migrations/add_loyalty_fields.py`
- ✅ Проверяет синтаксис Python файлов
- ✅ Проверяет наличие `.env`
- ✅ Запускает сервис

### Шаг 4: Проверка

```bash
# Проверить статус сервиса
systemctl status momsclub

# Проверить логи
journalctl -u momsclub -f --lines=50

# Проверить работу бота
# Отправьте сообщение боту в Telegram
```

## ⚠️ Откат на предыдущую версию

Если что-то пошло не так, можно быстро откатиться:

```bash
cd /root/home/momsclub/deploy
sudo ./rollback.sh 04112025
```

**Что делает скрипт:**
- ✅ Создает бэкап ТЕКУЩЕЙ (проблемной) версии
- ✅ Останавливает сервис
- ✅ Восстанавливает файлы из указанного бэкапа
- ✅ Опционально восстанавливает базу данных
- ✅ Запускает сервис

**Важно:** При откате вам будет предложено восстановить базу данных. Будьте осторожны! Если в новой версии были изменения в БД, они будут потеряны.

## 📁 Структура бэкапа

Бэкап содержит:
```
momsclub04112025/
├── bot.py
├── handlers/
├── database/
├── loyalty/
├── momsclub.db          # База данных
├── .env.backup          # Переменные окружения
└── ... (все остальные файлы)
```

## 🔍 Проверка перед деплоем

Рекомендуется перед деплоем:

1. **Проверить код локально:**
   ```bash
   python -m py_compile bot.py handlers/*.py loyalty/*.py
   ```

2. **Проверить миграцию:**
   ```bash
   python database/migrations/add_loyalty_fields.py
   ```

3. **Проверить зависимости:**
   ```bash
   pip install -r requirements.txt --dry-run
   ```

## 🆘 В случае проблем

1. **Сервис не запускается:**
   ```bash
   systemctl status momsclub
   journalctl -u momsclub -n 100
   ```

2. **Ошибка миграции:**
   - Проверьте логи выполнения миграции
   - Убедитесь, что база данных доступна
   - Проверьте права доступа к файлам

3. **Проблемы с зависимостями:**
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt --upgrade
   ```

4. **Откат:**
   ```bash
   ./rollback.sh [дата_бэкапа]
   ```

## 📝 Заметки

- Все скрипты должны запускаться от root или с sudo
- Бэкапы хранятся в `/root/home/momsclub[дата]`
- Старые бэкапы можно удалять вручную, когда убедитесь, что они не нужны
- Рекомендуется делать бэкап БД отдельно перед миграцией

## 🔐 Безопасность

- Скрипты не изменяют файл `.env` автоматически
- База данных копируется, но не удаляется
- Текущая версия сохраняется перед откатом


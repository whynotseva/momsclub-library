# 📘 Полное руководство по деплою системы лояльности

## 🎯 Цель
Безопасно развернуть систему лояльности на сервере с возможностью отката.

---

## 📋 Предварительные требования

1. **Доступ к серверу** (SSH)
2. **Права root** (или sudo)
3. **Новые файлы загружены** на сервер в `/root/home/momsclub/`

---

## 🔄 Процесс деплоя (3 варианта)

### Вариант 1: Автоматический (рекомендуется)

**Если все файлы уже загружены на сервер:**

```bash
cd /root/home/momsclub
chmod +x deploy/full_deploy.sh
sudo ./deploy/full_deploy.sh
```

Скрипт автоматически:
1. Проверит текущее состояние
2. Создаст бэкап
3. Обновит зависимости
4. Выполнит миграцию (с подтверждением)
5. Запустит сервис

---

### Вариант 2: Пошаговый (с полным контролем)

**Шаг 1: Создание бэкапа**
```bash
cd /root/home/momsclub/deploy
chmod +x *.sh
sudo ./safe_deploy.sh $(date +%d%m%Y)
```
⏱️ Время: ~1-2 минуты  
⚠️ Бот будет остановлен на время копирования

**Шаг 2: Загрузка новой версии**
- Загрузите новые файлы в `/root/home/momsclub/`
- Или используйте `git pull` если настроен git

**Шаг 3: Развертывание**
```bash
cd /root/home/momsclub/deploy
sudo ./deploy_new_version.sh
```
⏱️ Время: ~2-3 минуты

**Шаг 4: Проверка**
```bash
systemctl status momsclub
journalctl -u momsclub -f
```

---

### Вариант 3: Ручной (максимальный контроль)

Выполняйте каждую команду отдельно с проверкой результата.

---

## 📁 Что будет создано при бэкапе

```
/root/home/momsclub04112025/
├── bot.py
├── handlers/
├── database/
├── loyalty/              ← Новая папка (если была)
├── momsclub.db          ← База данных
├── .env.backup          ← Переменные окружения
└── ... (все остальные файлы)
```

---

## ✅ Чеклист после деплоя

После завершения деплоя проверьте:

- [ ] Сервис запущен: `systemctl status momsclub`
- [ ] В логах нет ошибок: `journalctl -u momsclub -n 50`
- [ ] Ночной крон запущен: `tail -f bot.log | grep loyalty`
- [ ] Админ-панель работает: `/admin` → "💎 Система лояльности"
- [ ] Личный кабинет показывает статус лояльности
- [ ] Миграция выполнена (в логах миграции)

---

## 🆘 Решение проблем

### Проблема: Сервис не запускается

```bash
# Проверить логи
journalctl -u momsclub -n 100

# Проверить конфигурацию
systemctl status momsclub

# Проверить файл сервиса
cat /etc/systemd/system/momsclub.service
```

### Проблема: Ошибка миграции

```bash
# Проверить логи миграции
cd /root/home/momsclub
python3 database/migrations/add_loyalty_fields.py

# Если ошибка - откатиться
cd deploy
sudo ./rollback.sh [дата]
```

### Проблема: Бот не отвечает

```bash
# Проверить процесс
ps aux | grep python | grep bot

# Перезапустить вручную
systemctl restart momsclub

# Проверить токен
grep BOT_TOKEN .env
```

---

## 🔙 Откат на предыдущую версию

```bash
cd /root/home/momsclub/deploy
sudo ./rollback.sh 04112025
```

**Что произойдёт:**
1. Сохранится текущая (проблемная) версия
2. Восстановятся файлы из бэкапа
3. Опционально восстановится база данных
4. Запустится сервис

⚠️ **Внимание:** При восстановлении БД будут потеряны изменения, сделанные после миграции!

---

## 📊 Ожидаемое время

- **Бэкап:** ~1-2 минуты
- **Загрузка файлов:** зависит от скорости (если не через git)
- **Миграция:** ~30-60 секунд (зависит от количества пользователей)
- **Развертывание:** ~2-3 минуты
- **Итого:** ~5-10 минут

---

## 🎯 Финальная проверка

После деплоя выполните:

```bash
# 1. Проверить статус
systemctl status momsclub

# 2. Проверить логи лояльности
tail -f /root/home/momsclub/bot.log | grep -i loyalty

# 3. Проверить миграцию в БД
sqlite3 /root/home/momsclub/momsclub.db "SELECT name FROM sqlite_master WHERE type='table' AND name='loyalty_events';"

# 4. Проверить поля в users
sqlite3 /root/home/momsclub/momsclub.db "PRAGMA table_info(users);" | grep loyalty
```

---

## ✅ Система готова, когда:

- [ ] Все шаги выполнены без ошибок
- [ ] Сервис работает
- [ ] В логах видно "Запуск ночного крона системы лояльности"
- [ ] Админ-панель работает
- [ ] Можно протестировать функции лояльности

---

**Готово! 🚀**


# Анализ текущего состояния и ТЗ на уведомления о смене платежной системы

## 📊 **Текущее состояние базы данных**

### Статистика:
- **Всего пользователей:** 643
- **Пользователей с платежными методами ЮКассы:** 95 
- **Активных подписок:** 108
- **Подписок, заканчивающихся в ближайшие 2 дня:** 2

### Проблема:
🚨 **95 пользователей имеют сохраненные платежные методы от старой системы ЮКасса, которые больше не работают с новой системой Prodamus**

## 🔍 **Анализ существующей системы уведомлений**

### Что уже есть:
1. **Система напоминаний для новых пользователей** (`get_users_for_reminder`)
   - Отправляет напоминание пользователям без подписки через 1 час после регистрации
   - Использует поле `reminder_sent` для отслеживания

2. **Система проверки истечения подписок** (`check_expired_subscriptions`)
   - Исключает пользователей из группы при истечении подписки
   - Работает через `GroupManager`

3. **Система автопродления** (`process_subscription_auto_renewal`) 
   - Пытается продлить подписки за 2 дня до окончания
   - НО: использует старые платежные методы ЮКассы!

### Что отсутствует:
❌ **Уведомления о смене платежной системы**
❌ **Предупреждения о необходимости обновления автопродления**

---

## 📋 **ТЕХНИЧЕСКОЕ ЗАДАНИЕ**

### **Цель:** 
Уведомить всех пользователей со старыми платежными методами ЮКассы о переходе на Prodamus и необходимости обновления автопродления.

---

## 🎯 **Задачи для реализации:**

### **1. Создание новой таблицы для отслеживания уведомлений**

```sql
CREATE TABLE migration_notifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    notification_type VARCHAR(50) NOT NULL, -- 'payment_system_migration'
    is_sent BOOLEAN DEFAULT FALSE,
    sent_at DATETIME NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

### **2. Новая функция поиска пользователей для уведомления**

```python
async def get_users_for_migration_notification(db: AsyncSession) -> List[User]:
    """
    Возвращает пользователей, которым нужно отправить уведомление о смене платежной системы:
    - payment_method_id НЕ NULL (есть старый метод ЮКассы)
    - is_recurring_active = True (включено автопродление) 
    - Имеют активную подписку ИЛИ подписка заканчивается в течение 7 дней
    - Еще не получали уведомление о миграции
    """
```

### **3. Новая задача в bot.py**

```python
async def send_migration_notifications():
    """
    Периодически отправляет уведомления о смене платежной системы
    Запускается раз в день в 10:00
    """
```

### **4. Текст уведомления**

```text
🔄 **Важное обновление Mom's Club!**

Дорогая мамочка! 💕

Мы обновили нашу платежную систему для вашего удобства и безопасности. 

⚠️ **Что это значит для вас:**
• Ваша текущая подписка действует до [ДАТА]
• Автопродление временно приостановлено
• Для следующего продления нужно заново настроить оплату

💳 **Что нужно сделать:**
1. За день до окончания подписки вы получите напоминание
2. Нажмите "Продлить подписку" 
3. Оплатите через новую безопасную систему
4. Автопродление снова заработает!

Спасибо за понимание! ❤️
```

### **5. Обновление автопродления**

- **Отключить автопродление** для пользователей со старыми платежными методами
- **Очистить payment_method_id** для всех пользователей с ЮКасса методами
- **Отправить уведомление** о необходимости обновления

### **6. Логика отправки**

1. **Целевая аудитория:**
   - Пользователи с `payment_method_id` != NULL
   - `is_recurring_active` = True  
   - Активная подписка ИЛИ заканчивается в течение 7 дней

2. **Частота отправки:**
   - Однократно при первом запуске
   - Повторно за 3 дня до окончания подписки (если не обновили)
   - Повторно за 1 день до окончания подписки (если не обновили)

3. **Условия остановки:**
   - Пользователь оформил новую подписку через Prodamus
   - Пользователь отключил автопродление
   - Подписка закончилась

---

## 💾 **План миграции данных:**

### **Этап 1: Подготовка (немедленно)**
```sql
-- Отключаем автопродление для всех пользователей со старыми методами
UPDATE users 
SET is_recurring_active = FALSE 
WHERE payment_method_id IS NOT NULL;

-- Помечаем старые методы как недействительные
UPDATE users 
SET payment_method_id = NULL
WHERE payment_method_id IS NOT NULL;
```

### **Этап 2: Уведомления (в течение недели)**
- Отправка уведомлений о смене системы
- Объяснение необходимости обновления

### **Этап 3: Мониторинг (постоянно)**
- Отслеживание пользователей, обновивших методы оплаты
- Повторные уведомления для не обновивших

---

## 🎛️ **Настройки для администраторов:**

```python
# В constants.py
MIGRATION_NOTIFICATION_SETTINGS = {
    'enabled': True,  # Включить/выключить уведомления
    'first_notification_delay_hours': 2,  # Через сколько часов после обнаружения
    'reminder_before_expiry_days': [7, 3, 1],  # За сколько дней напоминать
    'max_notifications_per_user': 5,  # Максимум уведомлений одному пользователю
}
```

---

## 📈 **Ожидаемые результаты:**

1. **Все 95 пользователей** получат уведомления о смене системы
2. **Автопродление безопасно отключено** для всех со старыми методами
3. **Пользователи проинформированы** о необходимости обновления
4. **Плавный переход** на новую платежную систему без потери подписчиков
5. **Предотвращение ошибок** автопродления со старыми методами

---

## ⏱️ **Сроки реализации:**

- **Миграция БД:** 1 день
- **Разработка уведомлений:** 2 дня  
- **Тестирование:** 1 день
- **Запуск:** немедленно после готовности

**Общий срок:** 4 дня

---

## 🔧 **Техническая реализация:**

1. **Создать миграцию БД** для новой таблицы уведомлений
2. **Добавить функции** в `database/crud.py`
3. **Создать задачу** в `bot.py` для отправки уведомлений  
4. **Обновить автопродление** для обработки новой ситуации
5. **Добавить логирование** для мониторинга процесса

**Все готово для утверждения и начала реализации!** ✅

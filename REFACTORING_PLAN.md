# 📋 ПЛАН РЕФАКТОРИНГА БИБЛИОТЕКИ MOM'S CLUB

> Версия: 1.0  
> Дата: 08.12.2025  
> Статус: В разработке

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### Frontend (Next.js 14)
| Файл | Строки | Проблема |
|------|--------|----------|
| `library/page.tsx` | 1463 | Монолит, всё в одном файле |
| `admin/page.tsx` | 2403 | Огромный файл, сложно поддерживать |
| `favorites/page.tsx` | ~200 | Дублирование логики с library |
| `history/page.tsx` | ~200 | Дублирование логики с library |

### Backend (FastAPI)
| Файл | Строки | Проблема |
|------|--------|----------|
| `materials.py` | 864 | Смешана бизнес-логика и роуты |
| `admin.py` | 500 | Можно разбить на модули |
| `push.py` | 413 | ОК, но можно улучшить |

---

## 🎯 ЦЕЛИ РЕФАКТОРИНГА

1. **Масштабируемость** — легко добавлять новые фичи
2. **Читаемость** — любой разработчик поймёт код за 15 минут
3. **Тестируемость** — возможность писать unit-тесты
4. **Переиспользование** — компоненты можно использовать повторно
5. **Производительность** — быстрая загрузка и отзывчивость

---

## 🔴 ПОДВОДНЫЕ КАМНИ И РИСКИ

### 1. Потеря функционала
**Риск:** При разбиении файла можно потерять связи между компонентами
**Решение:** 
- Делаем бэкап перед каждым этапом
- Тестируем после каждого изменения
- Сохраняем git историю

### 2. Сломанные импорты
**Риск:** При переносе компонентов сломаются пути импорта
**Решение:**
- Используем алиасы `@/components`, `@/hooks`, `@/lib`
- Проверяем `npm run build` после каждого этапа

### 3. Потеря состояния (State)
**Риск:** При разбиении компонентов состояние может не пробрасываться
**Решение:**
- Документируем все useState перед разбиением
- Используем React Context для глобального состояния
- Пробрасываем props явно

### 4. Проблемы с SSR (Server Side Rendering)
**Риск:** Next.js может ломаться при неправильном разбиении
**Решение:**
- Все компоненты с `useState`, `useEffect` помечаем `'use client'`
- Динамические импорты для тяжёлых компонентов

### 5. Потеря стилей
**Риск:** Tailwind классы могут не подхватиться в новых файлах
**Решение:**
- Проверяем `tailwind.config.ts` — все пути включены
- Тестируем визуально после каждого этапа

### 6. Проблемы с авторизацией
**Риск:** При разбиении может сломаться проверка токена
**Решение:**
- Выносим логику авторизации в отдельный хук `useAuth`
- Используем единый API клиент из `@/lib/api`

### 7. WebSocket соединения
**Риск:** `usePresence` может не работать в дочерних компонентах
**Решение:**
- Держим WebSocket на уровне страницы
- Пробрасываем данные через props или context

### 8. Push-уведомления
**Риск:** Service Worker может сломаться
**Решение:**
- Не трогаем `/public/sw.js`
- `usePushNotifications` остаётся без изменений

---

## 📁 ЦЕЛЕВАЯ СТРУКТУРА ПРОЕКТА

```
library_frontend/
├── app/
│   ├── library/
│   │   └── page.tsx          # ~100 строк (только композиция)
│   ├── admin/
│   │   └── page.tsx          # ~100 строк (только композиция)
│   ├── favorites/
│   │   └── page.tsx
│   ├── history/
│   │   └── page.tsx
│   ├── login/
│   │   └── page.tsx
│   └── layout.tsx
│
├── components/
│   ├── library/              # Компоненты библиотеки
│   │   ├── Header.tsx
│   │   ├── ProfileCard.tsx
│   │   ├── SubscriptionCard.tsx
│   │   ├── QuoteOfDay.tsx
│   │   ├── MaterialCard.tsx
│   │   ├── MaterialsGrid.tsx
│   │   ├── CategoryFilter.tsx
│   │   ├── SearchBar.tsx
│   │   ├── NotificationPanel.tsx
│   │   ├── RecommendationsSection.tsx
│   │   ├── FeaturedSection.tsx
│   │   ├── MobileNav.tsx
│   │   └── PushPromoModal.tsx
│   │
│   ├── admin/                # Компоненты админки
│   │   ├── StatsTab.tsx
│   │   ├── MaterialsTab.tsx
│   │   ├── CategoriesTab.tsx
│   │   ├── UsersTab.tsx
│   │   ├── HistoryTab.tsx
│   │   ├── MaterialForm.tsx
│   │   ├── PushForm.tsx
│   │   └── OnlineUsers.tsx
│   │
│   ├── shared/               # Общие компоненты
│   │   ├── Avatar.tsx
│   │   ├── Badge.tsx
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Modal.tsx
│   │   ├── ProgressBar.tsx
│   │   └── Skeleton.tsx
│   │
│   └── ui/                   # UI примитивы
│       ├── index.ts
│       └── ...
│
├── hooks/                    # Кастомные хуки
│   ├── useAuth.ts            # Авторизация
│   ├── useMaterials.ts       # Загрузка материалов
│   ├── useFavorites.ts       # Избранное
│   ├── useNotifications.ts   # Уведомления
│   ├── useCategories.ts      # Категории
│   ├── useStats.ts           # Статистика пользователя
│   ├── usePresence.ts        # WebSocket (уже есть)
│   └── usePushNotifications.ts # Push (уже есть)
│
├── lib/
│   ├── api.ts                # API клиент (уже есть)
│   ├── constants.ts          # Все константы
│   ├── types.ts              # TypeScript типы
│   ├── utils.ts              # Утилиты
│   └── quotes.ts             # 305 цитат
│
├── contexts/                 # React Context
│   ├── AuthContext.tsx
│   └── UserContext.tsx
│
└── styles/
    └── globals.css
```

---

## 📅 ПЛАН РАБОТ ПО ЭТАПАМ

### ЭТАП 0: Подготовка (30 мин) ✅ ВЫПОЛНЕНО 08.12.2025
- [x] Создать план ТЗ
- [x] Сделать бэкап на сервере (`/root/home/backups/library_backup_20251208_before_refactor.tar.gz`)
- [x] Залить проект на GitHub (`https://github.com/whynotseva/momsclub`)

### ЭТАП 1: Вынос констант и типов (1-2 часа) 🔄 В ПРОЦЕССЕ
**Что сделано:**
1. ✅ Создан `lib/constants.ts` — ADMIN_IDS, LOYALTY_BADGES, DEFAULT_USER, ITEMS_PER_PAGE, EXTERNAL_URLS
2. ✅ Создан `lib/types.ts` — Material, Category, Notification, UserDisplay, Stats, Activity, AdminAction
3. ✅ Создан `lib/quotes.ts` — 305 цитат + функции getDayOfYear, getQuoteOfDay, getRandomQuote
4. ⏸️ Обновление импортов в `library/page.tsx` — ОТЛОЖЕНО (требует аккуратной работы)

**Проверка:**
- ✅ `npm run build` проходит без ошибок
- ✅ Тестирование на сервере — **08.12.2025 13:32** — всё работает

**Подводные камни:**
- ⚠️ При замене в page.tsx нужно удалять старые определения ПОСЛЕ добавления импортов
- ⚠️ Типы в page.tsx немного отличаются от централизованных (категории вложенные)

---

### ЭТАП 2: Создание базовых хуков (2-3 часа) 🔄 В ПРОЦЕССЕ
**Что сделано:**
1. ✅ `useAuth.ts` — проверка токена, данные пользователя, logout, refreshUser
2. ✅ `useMaterials.ts` — загрузка материалов, recordView
3. ✅ `useFavorites.ts` — isFavorite, toggleFavorite
4. ✅ `useNotifications.ts` — markAsRead, markAllAsRead
5. ✅ `useCategories.ts` — загрузка категорий
6. ⏸️ Интеграция хуков в page.tsx — ОТЛОЖЕНО

**Проверка:**
- ✅ `npm run build` проходит без ошибок
- ✅ Тестирование на сервере — **08.12.2025 13:43** — всё работает

**Подводные камни:**
- ⚠️ Хуки готовы, но пока не используются в page.tsx
- ⚠️ При интеграции — заменять логику постепенно

---

### ЭТАП 3: Разбиение library/page.tsx (4-6 часов) 🔄 В ПРОЦЕССЕ
**Что сделано:**
1. ✅ QuoteOfDay подключен — **-358 строк** (1462 → 1104)
2. ⏸️ Остальные компоненты готовы, но не подключены

**Проверка:**
- ✅ `npm run build` проходит без ошибок
- ✅ Тестирование на сервере — **08.12.2025 14:10** — цитата работает

**Порядок выноса компонентов:**

1. **Header.tsx** (~80 строк)
   - Логотип, навигация, аватар
   - Props: user, isAdmin, notifications, onLogout

2. **NotificationPanel.tsx** (~100 строк)
   - Список уведомлений, push toggle
   - Props: notifications, onMarkRead, pushState

3. **ProfileCard.tsx** (~60 строк)
   - Приветствие, статистика, лояльность
   - Props: user, stats

4. **SubscriptionCard.tsx** (~50 строк)
   - Дней осталось, прогресс-бар, кнопка продления
   - Props: daysLeft, total

5. **QuoteOfDay.tsx** (~40 строк)
   - Цитата, кнопка обновления
   - Props: quote, onRefresh

6. **CategoryFilter.tsx** (~80 строк)
   - Сетка кнопок категорий
   - Props: categories, active, onChange

7. **MaterialCard.tsx** (~80 строк)
   - Карточка материала
   - Props: material, isFavorite, onOpen, onToggleFavorite

8. **FeaturedSection.tsx** (~50 строк)
   - Горизонтальный скролл "Выбор Полины"
   - Props: materials, onOpen

9. **RecommendationsSection.tsx** (~50 строк)
   - AI рекомендации
   - Props: recommendations, onOpen

10. **MobileNav.tsx** (~40 строк)
    - Нижняя навигация
    - Props: activePage, isPWA

11. **PushPromoModal.tsx** (~60 строк)
    - Модалка включения push
    - Props: show, onEnable, onDismiss

**Проверка после каждого компонента:**
- `npm run build`
- Визуальная проверка в браузере
- Все интерактивные элементы работают

---

### ЭТАП 4: Разбиение admin/page.tsx (4-6 часов)
**Порядок:**

1. **StatsTab.tsx** — статистика и графики
2. **MaterialsTab.tsx** — таблица материалов, CRUD
3. **MaterialForm.tsx** — форма создания/редактирования
4. **CategoriesTab.tsx** — управление категориями
5. **UsersTab.tsx** — список пользователей, модалка
6. **HistoryTab.tsx** — лог действий админов
7. **PushForm.tsx** — форма отправки push

---

### ЭТАП 5: Оптимизация производительности (2-3 часа)
**Что делаем:**
1. `React.memo()` для тяжёлых компонентов
2. `useMemo()` для вычисляемых значений
3. `useCallback()` для функций в props
4. Lazy loading для вкладок админки
5. Виртуализация списков (если >100 материалов)

---

### ЭТАП 6: Создание общих UI компонентов (2-3 часа)
**Если нужно:**
1. Button с вариантами (primary, secondary, ghost)
2. Card с единым стилем
3. Modal с анимациями
4. Skeleton для загрузки
5. Badge для статусов

---

### ЭТАП 7: Backend рефакторинг (опционально, 4-6 часов)
**Что делаем:**
1. Выносим бизнес-логику в `services/`
2. Разбиваем `materials.py` на модули
3. Добавляем кэширование Redis (если нужно)
4. Оптимизируем SQL запросы

---

## 🧪 ЧЕКЛИСТ ТЕСТИРОВАНИЯ (ОБЯЗАТЕЛЬНО!)

> ⚠️ **После КАЖДОГО изменения — проверить вручную на сервере!**

### Быстрая проверка (после каждого деплоя):
1. Открыть https://librarymomsclub.ru/library
2. Страница загружается без ошибок
3. Материалы отображаются
4. Цитата дня показывается
5. Фильтры категорий работают
6. Поиск работает
7. Кнопка лайка работает
8. Админка открывается: https://librarymomsclub.ru/admin

### Полная проверка (после крупных изменений):
- [ ] `npm run build` проходит без ошибок
- [ ] `npm run lint` без критических ошибок
- [ ] Страница библиотеки загружается
- [ ] Авторизация работает
- [ ] Материалы отображаются
- [ ] Фильтры работают
- [ ] Поиск работает
- [ ] Лайки работают
- [ ] Просмотры записываются
- [ ] Уведомления показываются
- [ ] Push работает
- [ ] Мобильная версия ОК
- [ ] Админка работает

---

## ⏱️ ОБЩАЯ ОЦЕНКА ВРЕМЕНИ

| Этап | Часы | Приоритет |
|------|------|-----------|
| Этап 0: Подготовка | 0.5 | 🔴 |
| Этап 1: Константы и типы | 1-2 | 🔴 |
| Этап 2: Хуки | 2-3 | 🔴 |
| Этап 3: library/page.tsx | 4-6 | 🔴 |
| Этап 4: admin/page.tsx | 4-6 | 🟡 |
| Этап 5: Оптимизация | 2-3 | 🟢 |
| Этап 6: UI компоненты | 2-3 | 🟢 |
| Этап 7: Backend | 4-6 | 🟢 |
| **ИТОГО** | **20-30** | |

---

## 📝 ЗАМЕТКИ

- Каждый этап — отдельный commit с описанием
- После этапов 1-4 — деплой на сервер для проверки
- При критической ошибке — откат из бэкапа
- Документируем все решения в этом файле

---

## 📦 БЭКАПЫ

| Дата | Название | Описание |
|------|----------|----------|
| 08.12.2025 | `library_backup_20251208_before_refactor` | До начала рефакторинга |
| | | |


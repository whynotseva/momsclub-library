# ТЗ: Аудит кода библиотеки MomsClub

## Описание проекта

**LibriMomsClub** — закрытая библиотека материалов для мам-блогеров с подпиской.

### Стек технологий

**Backend (library_backend/):**
- Python 3.10+ / FastAPI
- SQLAlchemy + SQLite
- JWT авторизация (интеграция с Telegram Login Widget)
- WebSocket для real-time активности

**Frontend (library_frontend/):**
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- PWA (Service Worker)

---

## Задача

Провести **код-ревью и аудит** проекта на предмет:

### 1. Логические ошибки
- Конфликты имён функций/переменных (пример найденного бага: `check_admin` перекрывала импорт)
- Неправильная обработка edge cases
- Race conditions в async коде
- Утечки памяти

### 2. Безопасность
- SQL-инъекции (проверить все `text()` запросы)
- XSS уязвимости на фронтенде
- Проверка JWT токенов
- Утечка чувствительных данных в логах

### 3. Производительность
- N+1 запросы к БД
- Отсутствие индексов
- Тяжёлые операции в sync контексте
- Неоптимальные запросы

### 4. Качество кода
- Дублирование кода
- Нарушение DRY/SOLID принципов
- Отсутствие обработки ошибок
- Неконсистентность стиля

---

## Критичные файлы для проверки

### Backend
```
library_backend/
├── app/
│   ├── api/
│   │   ├── dependencies.py    # Проверка авторизации и подписки
│   │   ├── materials.py       # Основные эндпоинты материалов
│   │   ├── auth.py            # Авторизация через Telegram
│   │   ├── favorites.py       # Избранное
│   │   └── websocket.py       # WebSocket соединения
│   ├── services/
│   │   └── material_service.py
│   └── utils/
│       └── auth.py            # JWT логика
└── main.py
```

### Frontend
```
library_frontend/
├── app/
│   ├── login/page.tsx         # Авторизация
│   ├── library/page.tsx       # Главная страница
│   └── admin/page.tsx         # Админка
├── components/
│   └── library/               # UI компоненты
├── contexts/
│   └── AuthContext.tsx        # Контекст авторизации
├── hooks/
│   └── useMaterials.ts        # Хук загрузки материалов
└── lib/
    └── api.ts                 # API клиент
```

---

## Известные проблемы (уже исправлены)

1. **Конфликт имён `check_admin`** — локальная функция перекрывала импортированную, все пользователи получали 403 на `/api/materials`. Исправлено переименованием в `require_admin`.

2. **Авто-редирект при 403** — создавал замкнутый круг при проблемах с подпиской. Убран редирект при 403, оставлен только при 401.

---

## Формат отчёта

По каждой найденной проблеме:

```
### [КРИТИЧНОСТЬ] Название проблемы

**Файл:** путь/к/файлу.py:строка
**Тип:** безопасность / логика / производительность / качество

**Описание:**
Что не так и почему это проблема.

**Решение:**
Как исправить (с примером кода если нужно).
```

Критичность:
- 🔴 **CRITICAL** — нужно исправить срочно
- 🟠 **HIGH** — важно исправить
- 🟡 **MEDIUM** — желательно исправить
- 🟢 **LOW** — рекомендация

---

## Контакты

По вопросам обращаться к владельцу репозитория.

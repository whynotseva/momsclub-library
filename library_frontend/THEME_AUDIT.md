# 🔴 АУДИТ ТЕМ: Светлая и Тёмная тема

## Проблема
Темы перемешались:
- В светлой теме видны элементы тёмной темы (тёмный текст, тёмные блоки)
- В тёмной теме видны элементы светлой темы (светлый текст, светлые блоки)

---

## Архитектура тем (как должно работать)

### 1. ThemeContext (`contexts/ThemeContext.tsx`)
- Хранит состояние темы: `'light' | 'dark' | 'system'`
- Сохраняет в `localStorage.theme`
- Добавляет/удаляет класс `dark` на `<html>`

### 2. Tailwind CSS (`tailwind.config.ts`)
- Должен быть `darkMode: 'class'`
- Все стили через `dark:` префиксы

### 3. Правило стилей
```
СВЕТЛАЯ ТЕМА: базовые классы без префикса
ТЁМНАЯ ТЕМА: классы с префиксом dark:
```

---

## Файлы для аудита

### Библиотека (основной сайт)
| Файл | Что проверить |
|------|---------------|
| `app/library/page.tsx` | Все цвета текста, фоны, бордеры |
| `components/library/Header.tsx` | Шапка, навигация |
| `components/library/MaterialCard.tsx` | Карточки материалов |
| `components/library/FeaturedSection.tsx` | Секция "Выбор Полины" |
| `app/favorites/page.tsx` | Страница избранного |
| `app/history/page.tsx` | Страница истории |
| `app/profile/page.tsx` | Страница профиля |

### Админка
| Файл | Что проверить |
|------|---------------|
| `app/admin/page.tsx` | Шапка, табы, все секции |
| `components/admin/StatsTab.tsx` | Статистика |
| `components/admin/MaterialsTab.tsx` | Материалы |
| `components/admin/CategoriesTab.tsx` | Категории |
| `components/admin/HistoryTab.tsx` | История |
| `components/admin/UsersTab.tsx` | Пользователи |
| `components/admin/BotStatsTab.tsx` | Бот статистика |
| `components/admin/MaterialFormModal.tsx` | Модалка материала |
| `components/admin/CategoryFormModal.tsx` | Модалка категории |
| `components/admin/UserDetailsModal.tsx` | Модалка пользователя |
| `components/admin/BotUserCard.tsx` | Карточка бот-пользователя |

### Общие компоненты
| Файл | Что проверить |
|------|---------------|
| `components/shared/Avatar.tsx` | Аватар |
| `components/shared/LoadingSpinner.tsx` | Лоадер |
| `components/shared/SubscriptionGuard.tsx` | Гард подписки |

### Глобальные стили
| Файл | Что проверить |
|------|---------------|
| `app/globals.css` | Глобальные стили, body background |
| `tailwind.config.ts` | darkMode: 'class' |

---

## Цвета по темам

### СВЕТЛАЯ ТЕМА (базовые классы)
```
Фон страницы:     bg-gradient-to-b from-[#FDFCFA] via-[#FBF8F3] to-[#F5EFE6]
Фон карточки:     bg-white/80, bg-white/90
Текст основной:   text-[#2D2A26], text-[#5D4E3A]
Текст вторичный:  text-[#8B8279]
Бордер:           border-[#E8D4BA]/30, border-[#E8D4BA]/40
Акцент:           text-[#B08968], bg-[#B08968]
```

### ТЁМНАЯ ТЕМА (dark: префиксы)
```
Фон страницы:     dark:from-[#121212] dark:via-[#1A1A1A] dark:to-[#121212]
Фон карточки:     dark:bg-[#1E1E1E]/80, dark:bg-[#2A2A2A]
Текст основной:   dark:text-[#E5E5E5]
Текст вторичный:  dark:text-[#B0B0B0]
Бордер:           dark:border-[#3D3D3D]
Акцент:           #B08968 (одинаковый)
```

---

## Чеклист проверки каждого файла

Для каждого файла нужно выполнить grep и проверить что ВСЕ элементы имеют пару светлая + тёмная:

```bash
# 1. Найти элементы БЕЗ dark: варианта (потенциальные проблемы)
grep -n "bg-white\|bg-\[#F" file.tsx | grep -v "dark:"
grep -n "text-\[#5D4E3A\]\|text-\[#2D2A26\]\|text-\[#8B8279\]" file.tsx | grep -v "dark:"
grep -n "border-\[#E8D4BA\]" file.tsx | grep -v "dark:"

# 2. Найти тёмные стили БЕЗ светлого варианта
grep -n "dark:bg-\[#1\|dark:bg-\[#2" file.tsx
grep -n "dark:text-\[#E\|dark:text-\[#B0\]" file.tsx
```

---

## Шаги исправления

### Шаг 1: Проверить ThemeContext
- [ ] `mounted` состояние работает корректно
- [ ] Класс `dark` добавляется на `<html>` при выборе тёмной темы
- [ ] localStorage сохраняет и читает тему

### Шаг 2: Проверить tailwind.config.ts
- [ ] `darkMode: 'class'` установлен

### Шаг 3: Аудит globals.css
- [ ] Нет жёстких цветов без dark: варианта
- [ ] body background имеет оба варианта

### Шаг 4: Аудит каждой страницы
Для каждого файла из списка:
1. Открыть файл
2. Прогнать grep команды
3. Для каждого элемента без dark: — добавить dark: вариант
4. Для каждого dark: без базового — добавить базовый

### Шаг 5: Тестирование
- [ ] Открыть в светлой теме — все элементы светлые
- [ ] Переключить на тёмную — все элементы тёмные
- [ ] Переключить обратно — все элементы светлые
- [ ] Перезагрузить страницу — тема сохранилась

---

## Приоритет исправления

1. **КРИТИЧНО**: `app/library/page.tsx` — главная страница
2. **КРИТИЧНО**: `components/library/Header.tsx` — шапка везде
3. **ВАЖНО**: `app/admin/page.tsx` — админка
4. **ВАЖНО**: Все компоненты библиотеки
5. **СРЕДНЕ**: Компоненты админки
6. **НИЗКО**: Модалки и второстепенные компоненты

---

## Дата создания: 20.12.2024
## Статус: ОЖИДАЕТ ВЫПОЛНЕНИЯ

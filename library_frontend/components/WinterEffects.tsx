'use client'

// ❄️ ЗИМНИЙ РЕЖИМ — стили теперь в globals.css
export const WINTER_MODE_ENABLED = true

export default function WinterEffects() {
  // Эффекты теперь через CSS в globals.css
  return null
}

# 🎨 LibriMomsClub Frontend

Сайт библиотеки для мам-блогеров на Next.js 14 + React + TypeScript + TailwindCSS

## 📋 Что это?

Это **сайт** (то, что видят пользователи в браузере):
- Красивые страницы
- Формы, кнопки, карточки
- Авторизация через Telegram
- Каталог материалов
- Избранное, история

## 🛠️ Установка Node.js (если нет)

### macOS:
```bash
# Установить Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Node.js
brew install node
```

### Проверка:
```bash
node --version  # должно быть v18+
npm --version
```

## 🚀 Запуск локально

```bash
# 1. Установить зависимости
npm install

# 2. Создать .env.local файл
cp .env.example .env.local

# 3. Запустить dev сервер
npm run dev
```

Откроется: http://localhost:3000

## 📂 Структура

```
library_frontend/
├── app/                    # Страницы (Next.js App Router)
│   ├── layout.tsx         # Общий layout
│   ├── page.tsx           # Главная (/)
│   ├── login/             # Страница входа
│   ├── library/           # Каталог материалов
│   ├── favorites/         # Избранное
│   └── history/           # История
├── components/            # React компоненты
│   ├── ui/               # UI компоненты (кнопки, карточки)
│   ├── layout/           # Layout компоненты (header, footer)
│   └── library/          # Компоненты библиотеки
├── lib/                  # Утилиты
│   ├── api.ts           # API клиент
│   └── auth.ts          # Авторизация
├── public/              # Статика (картинки, иконки)
└── styles/              # CSS
```

## 🎨 Технологии

- **Next.js 14** — фреймворк для React
- **React 18** — библиотека для UI
- **TypeScript** — типизация
- **TailwindCSS** — стили
- **Lucide Icons** — иконки

## 📝 Примечания

**Backend API должен быть запущен** на http://localhost:8001

Сайт подключается к API и показывает данные красиво! ✨

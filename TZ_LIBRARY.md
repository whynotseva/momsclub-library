# 📚 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: LibriMomsClub.ru

**Проект:** LibriMomsClub — закрытая библиотека для мам-креаторов  
**Заказчик:** MomsClub (Полина Дмитренко)  
**Куратор:** Всеволод Дмитренко  
**Дата:** 30 ноября 2025  
**Версия:** 2.0 (обновлённая концепция)

---

## 1. О ПРОЕКТЕ MOM'S CLUB

### 1.1 Что такое Mom's Club
**Mom's Club** — это закрытое пространство для мам, которые растут как:
- 📱 Блогеры и контент-креаторы
- 💼 Эксперты и специалисты
- 🎨 Творческие предприниматели
- 🌟 Личные бренды

Это место, где женщина сочетает **материнство, творчество, контент и личный бренд** — и получает поддержку, идеи, инструменты и вдохновение.

### 1.2 Что внутри Mom's Club
- 🎞️ Подборки вирусных Reels и идеи для контента
- 📝 Идеи для постов и сторис по нишам
- 💡 Лайфхаки по блогингу и личному бренду
- 🏆 Челленджи и контент-марафоны
- 🎙️ Подкасты и разборы аккаунтов
- 💬 Поддержка контента и обратная связь
- 🤝 Уютное комьюнити мам-креаторов
- 🤝 Предложения от брендов для сотрудничества

### 1.3 Цель LibriMomsClub
**LibriMomsClub** — это закрытая онлайн-библиотека всей этой «начинки».

Создать премиальную библиотеку, которая позволит участницам:
- ✅ Быстро находить идеи для Reels, постов, сторис
- ✅ Учиться блогингу, упаковке и личному бренду
- ✅ Выполнять челленджи и креативные задания
- ✅ Смотреть разборы аккаунтов и слушать подкасты
- ✅ Получать материалы для роста как блогера
- ✅ Сохранять материалы для вдохновения
- ✅ Иметь единый центр всех образовательных материалов

### 1.4 Проблема (почему не Notion)
Текущая библиотека в Notion:
- ❌ Неудобна в использовании (тормозит)
- ❌ Нет контроля доступа (любой с ссылкой может зайти)
- ❌ Нет аналитики (кто, когда, что смотрит)
- ❌ Нет интеграции с системой подписок бота
- ❌ Нет поиска и фильтров
- ❌ Не адаптирована под мобильные устройства

---

## 2. ЦЕЛЕВАЯ АУДИТОРИЯ

### 2.1 Основная ЦА
- 👩‍💼 Мамы-креаторы (блогеры, контент-мейкеры)
- 📸 Мамы-эксперты (психологи, коучи, нутрициологи)
- 💼 Мамы-предприниматели, которые ведут блог
- 🌟 Женщины, которые растят личный бренд

**Возраст:** 25-40 лет  
**Платформы:** Instagram, Telegram, TikTok  
**Уровень:** от новичков до продвинутых блогеров

### 2.2 Их запросы
- 🎯 Чёткие, удобные материалы
- 💡 Множество идей, трендов и шаблонов
- 📚 Единое пространство для вдохновения
- ⚡ Простота, скорость, мобильность
- 💬 Поддержка в контенте

### 2.3 Особенности поведения
- Заходит в перерывах между делами
- Часто в состоянии усталости/перегруза
- Ценит простоту, ясность, красивый UI
- Пользуется в основном мобилой (80%)
- Хочет быстро найти нужный материал (≤10 секунд)

---

## 3. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 2.1 Авторизация

#### 2.1.1 Вход через Telegram
- Пользователь открывает сайт библиотеки
- Видит кнопку "Войти через Telegram"
- После клика → Telegram Login Widget запрашивает подтверждение
- Backend получает `telegram_id`, `first_name`, `username`, `photo_url`
- Backend проверяет наличие активной подписки в БД
- Если подписка активна → создаётся JWT токен (срок жизни: 7 дней)
- Пользователь перенаправляется в библиотеку

#### 2.1.2 Проверка подписки
**При каждом запросе к защищённым endpoint'ам:**
```sql
SELECT s.is_active, s.end_date 
FROM subscriptions s 
JOIN users u ON s.user_id = u.id 
WHERE u.telegram_id = ? 
AND s.is_active = 1 
AND s.end_date > datetime('now')
```

**Логика:**
- Подписка активна → доступ разрешён
- Подписка истекла → редирект на `/subscribe` с сообщением
- Пользователя нет в БД → редирект на `/subscribe`

#### 2.1.3 Автоматический выход
- Если подписка истекла → при следующем запросе доступ закрывается
- Если пользователя кикнули из канала → бот деактивирует подписку → доступ закрывается
- JWT токен инвалидируется

---

### 2.2 Каталог материалов

#### 2.2.1 Главная страница библиотеки
**Отображение:**
- Сетка карточек материалов (3 колонки на desktop, 1 на mobile)
- Каждая карточка содержит:
  - Обложка (изображение)
  - Название материала
  - Краткое описание (2-3 строки)
  - Категория (тег)
  - Дата добавления
  - Кнопка "Открыть"

**Сортировка:**
- По умолчанию: новые сначала
- Опции: по дате, по названию, по популярности

#### 2.2.2 Категории
**Список категорий (примерный):**
- Воспитание детей
- Психология
- Здоровье и питание
- Развитие ребёнка
- Творчество и игры
- Финансы и карьера
- Отношения
- Саморазвитие

**Фильтрация:**
- Чекбоксы для выбора нескольких категорий
- Мгновенная фильтрация без перезагрузки страницы

#### 2.2.3 Поиск
- Строка поиска в шапке сайта
- Поиск по названию и описанию материала
- Подсветка найденных совпадений
- Показ результатов в реальном времени (debounce 300ms)

#### 2.2.4 Страница материала
**Содержимое:**
- Полное название
- Автор (если есть)
- Дата публикации
- Категория
- Полное описание
- Контент материала:
  - Текст (Markdown)
  - Изображения
  - Видео (YouTube/Vimeo embed)
  - PDF файлы (встроенный просмотр)
  - Ссылки на внешние ресурсы

**Дополнительно:**
- Кнопка "Назад в библиотеку"
- Кнопка "Поделиться" (копирует ссылку)
- Счётчик просмотров

---

### 2.3 Страница "Подписка истекла"

**Отображается когда:**
- У пользователя нет активной подписки
- Подписка истекла

**Содержимое:**
- Заголовок: "Подписка неактивна"
- Текст: "Для доступа к библиотеке необходима активная подписка Mom's Club"
- Информация о тарифах:
  - 1 месяц — 990₽
  - 2 месяца — 1790₽
  - 3 месяца — 2490₽
- Кнопка "Оформить подписку" → открывает бота в Telegram
- Ссылка на поддержку

---

## 3. ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### 3.1 Архитектура

```
┌─────────────────┐
│   Frontend      │  Next.js / React
│   (Vercel)      │  
└────────┬────────┘
         │
         │ HTTPS
         │
┌────────▼────────┐
│   Backend       │  FastAPI (Python)
│   (VPS Server)  │  
└────────┬────────┘
         │
         │
┌────────▼────────┐
│   Database      │  SQLite (существующая БД бота)
│   (VPS Server)  │  
└─────────────────┘
```

---

### 3.2 Backend (FastAPI)

#### 3.2.1 Технологии
- **Python 3.10+**
- **FastAPI** (уже используется в проекте)
- **SQLAlchemy** (уже используется)
- **JWT** для токенов (библиотека: `python-jose`)
- **CORS** для работы с frontend

#### 3.2.2 Новые endpoint'ы

**Авторизация:**
```python
POST /api/auth/telegram
Body: {
  "id": 123456789,
  "first_name": "Olga",
  "username": "olga_user",
  "photo_url": "https://...",
  "auth_date": 1234567890,
  "hash": "abc123..."  # Для проверки подлинности
}
Response: {
  "access_token": "eyJ...",
  "token_type": "bearer",
  "user": {
    "telegram_id": 123456789,
    "first_name": "Olga",
    "subscription_end": "2025-12-20T00:00:00"
  }
}
```

**Проверка подписки:**
```python
GET /api/auth/check
Headers: Authorization: Bearer <token>
Response: {
  "active": true,
  "end_date": "2025-12-20T00:00:00"
}
```

**Библиотека:**
```python
GET /api/library/materials
Headers: Authorization: Bearer <token>
Query params: 
  - category: string (optional)
  - search: string (optional)
  - page: int (default: 1)
  - limit: int (default: 20)
Response: {
  "materials": [
    {
      "id": 1,
      "title": "Как справиться с истериками",
      "description": "Практические советы...",
      "category": "Воспитание детей",
      "cover_image": "https://...",
      "created_at": "2025-11-20T10:00:00",
      "views": 145
    },
    ...
  ],
  "total": 50,
  "page": 1,
  "pages": 3
}

GET /api/library/materials/{id}
Headers: Authorization: Bearer <token>
Response: {
  "id": 1,
  "title": "Как справиться с истериками",
  "description": "Полное описание...",
  "content": "# Markdown контент\n\n...",
  "category": "Воспитание детей",
  "author": "Полина Иванова",
  "cover_image": "https://...",
  "attachments": [
    {"type": "pdf", "url": "https://..."},
    {"type": "video", "url": "https://youtube.com/..."}
  ],
  "created_at": "2025-11-20T10:00:00",
  "views": 145
}

POST /api/library/materials/{id}/view
Headers: Authorization: Bearer <token>
Response: {"success": true}
```

**Категории:**
```python
GET /api/library/categories
Response: {
  "categories": [
    {"id": 1, "name": "Воспитание детей", "count": 25},
    {"id": 2, "name": "Психология", "count": 18},
    ...
  ]
}
```

#### 3.2.3 Middleware для проверки подписки
```python
@app.middleware("http")
async def check_subscription_middleware(request: Request, call_next):
    if request.url.path.startswith("/api/library"):
        token = request.headers.get("Authorization", "").replace("Bearer ", "")
        
        try:
            payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
            telegram_id = payload["telegram_id"]
            
            # Проверяем подписку
            subscription = await get_active_subscription(telegram_id)
            if not subscription:
                return JSONResponse(
                    status_code=403,
                    content={"error": "Подписка неактивна"}
                )
            
            request.state.user = payload
        except JWTError:
            return JSONResponse(
                status_code=401,
                content={"error": "Невалидный токен"}
            )
    
    return await call_next(request)
```

#### 3.2.4 Новые таблицы в БД

```sql
CREATE TABLE library_materials (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    content TEXT,  -- Markdown
    category_id INTEGER,
    author TEXT,
    cover_image TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    views INTEGER DEFAULT 0,
    is_published BOOLEAN DEFAULT 1
);

CREATE TABLE library_categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    slug TEXT NOT NULL UNIQUE,
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE library_attachments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    material_id INTEGER NOT NULL,
    type TEXT NOT NULL,  -- 'pdf', 'video', 'image', 'link'
    url TEXT NOT NULL,
    title TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (material_id) REFERENCES library_materials(id)
);

CREATE TABLE library_views (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    material_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    viewed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (material_id) REFERENCES library_materials(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

### 3.3 Frontend (Next.js)

#### 3.3.1 Технологии
- **Next.js 14+** (App Router)
- **React 18+**
- **TypeScript**
- **TailwindCSS** (стилизация)
- **shadcn/ui** (компоненты)
- **Lucide Icons** (иконки)
- **React Query** (кеширование запросов)
- **Zustand** (state management)

#### 3.3.2 Структура страниц
```
/                      → Главная (редирект на /library если залогинен)
/login                 → Страница входа (Telegram Login Widget)
/library               → Каталог материалов (защищено)
/library/[id]          → Страница материала (защищено)
/subscribe             → Страница "Подписка истекла"
```

#### 3.3.3 Компоненты

**LoginButton.tsx**
```tsx
import { useEffect } from 'react';

export default function LoginButton() {
  useEffect(() => {
    // Инициализация Telegram Login Widget
    const script = document.createElement('script');
    script.src = 'https://telegram.org/js/telegram-widget.js?22';
    script.setAttribute('data-telegram-login', 'YOUR_BOT_USERNAME');
    script.setAttribute('data-size', 'large');
    script.setAttribute('data-onauth', 'onTelegramAuth(user)');
    script.setAttribute('data-request-access', 'write');
    script.async = true;
    document.getElementById('telegram-login').appendChild(script);
  }, []);

  return <div id="telegram-login"></div>;
}
```

**MaterialCard.tsx**
```tsx
interface MaterialCardProps {
  id: number;
  title: string;
  description: string;
  category: string;
  coverImage: string;
  createdAt: string;
}

export default function MaterialCard({ 
  id, title, description, category, coverImage, createdAt 
}: MaterialCardProps) {
  return (
    <Link href={`/library/${id}`}>
      <div className="border rounded-lg overflow-hidden hover:shadow-lg transition">
        <img src={coverImage} alt={title} className="w-full h-48 object-cover" />
        <div className="p-4">
          <span className="text-xs text-pink-600 font-semibold">{category}</span>
          <h3 className="text-lg font-bold mt-2">{title}</h3>
          <p className="text-sm text-gray-600 mt-2 line-clamp-2">{description}</p>
          <p className="text-xs text-gray-400 mt-2">{new Date(createdAt).toLocaleDateString('ru-RU')}</p>
        </div>
      </div>
    </Link>
  );
}
```

**ProtectedRoute.tsx**
```tsx
'use client';
import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { useAuth } from '@/hooks/useAuth';

export default function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const router = useRouter();
  const { isAuthenticated, isLoading, hasActiveSubscription } = useAuth();

  useEffect(() => {
    if (!isLoading) {
      if (!isAuthenticated) {
        router.push('/login');
      } else if (!hasActiveSubscription) {
        router.push('/subscribe');
      }
    }
  }, [isAuthenticated, isLoading, hasActiveSubscription, router]);

  if (isLoading) {
    return <div>Загрузка...</div>;
  }

  if (!isAuthenticated || !hasActiveSubscription) {
    return null;
  }

  return <>{children}</>;
}
```

#### 3.3.4 Хуки

**useAuth.ts**
```tsx
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  token: string | null;
  user: any | null;
  isAuthenticated: boolean;
  hasActiveSubscription: boolean;
  login: (token: string, user: any) => void;
  logout: () => void;
  checkSubscription: () => Promise<void>;
}

export const useAuth = create<AuthState>()(
  persist(
    (set, get) => ({
      token: null,
      user: null,
      isAuthenticated: false,
      hasActiveSubscription: false,
      
      login: (token, user) => {
        set({ token, user, isAuthenticated: true, hasActiveSubscription: true });
      },
      
      logout: () => {
        set({ token: null, user: null, isAuthenticated: false, hasActiveSubscription: false });
      },
      
      checkSubscription: async () => {
        const { token } = get();
        if (!token) return;
        
        try {
          const res = await fetch('/api/auth/check', {
            headers: { 'Authorization': `Bearer ${token}` }
          });
          
          if (!res.ok) {
            set({ hasActiveSubscription: false });
          } else {
            const data = await res.json();
            set({ hasActiveSubscription: data.active });
          }
        } catch (error) {
          set({ hasActiveSubscription: false });
        }
      }
    }),
    { name: 'auth-storage' }
  )
);
```

---

### 3.4 Дизайн

#### 3.4.1 Цветовая схема
- **Основной цвет:** #EC4899 (розовый, как в боте)
- **Акцентный:** #F472B6 (светло-розовый)
- **Фон:** #FFFFFF (белый)
- **Текст:** #1F2937 (тёмно-серый)
- **Вторичный текст:** #6B7280 (серый)

#### 3.4.2 Шрифты
- **Заголовки:** Inter Bold
- **Текст:** Inter Regular

#### 3.4.3 Адаптивность
- **Mobile:** 320px - 767px (1 колонка)
- **Tablet:** 768px - 1023px (2 колонки)
- **Desktop:** 1024px+ (3 колонки)

---

## 4. БЕЗОПАСНОСТЬ

### 4.1 Проверка подлинности Telegram данных
При получении данных от Telegram Login Widget необходимо проверить `hash`:

```python
import hashlib
import hmac

def verify_telegram_auth(data: dict, bot_token: str) -> bool:
    check_hash = data.pop('hash')
    data_check_string = '\n'.join([f'{k}={v}' for k, v in sorted(data.items())])
    secret_key = hashlib.sha256(bot_token.encode()).digest()
    calculated_hash = hmac.new(secret_key, data_check_string.encode(), hashlib.sha256).hexdigest()
    return calculated_hash == check_hash
```

### 4.2 JWT токены
- Алгоритм: HS256
- Срок жизни: 7 дней
- Payload: `{"telegram_id": 123456789, "exp": 1234567890}`
- Secret key: хранить в `.env`

### 4.3 HTTPS
- Обязательно использовать HTTPS для всех запросов
- SSL сертификат: Let's Encrypt (бесплатно)

### 4.4 Rate Limiting
- Максимум 100 запросов в минуту на IP
- Максимум 1000 запросов в час на пользователя

---

## 5. АДМИН-ПАНЕЛЬ

### 5.1 Управление материалами

**Добавить в существующую админку бота:**

**Команды:**
- `/library_add` — Добавить материал
- `/library_edit {id}` — Редактировать материал
- `/library_delete {id}` — Удалить материал
- `/library_stats` — Статистика (просмотры, популярные материалы)

**Процесс добавления материала:**
1. Админ отправляет `/library_add`
2. Бот запрашивает:
   - Название
   - Описание
   - Категорию (выбор из списка)
   - Обложку (фото)
   - Контент (текст в Markdown или файл)
   - Вложения (опционально)
3. Бот сохраняет в БД
4. Материал появляется в библиотеке

---

## 6. АНАЛИТИКА

### 6.1 Метрики
- Количество просмотров каждого материала
- Самые популярные материалы (топ-10)
- Активность пользователей (кто, когда заходил)
- Поисковые запросы (что ищут)

### 6.2 Отчёты для админа
```python
GET /api/admin/library/stats
Response: {
  "total_materials": 50,
  "total_views": 1250,
  "top_materials": [
    {"id": 5, "title": "...", "views": 145},
    ...
  ],
  "active_users_today": 23,
  "popular_searches": ["истерики", "сон", "питание"]
}
```

---

## 7. ЭТАПЫ РАЗРАБОТКИ

### Этап 1: Backend (3-4 дня)
- [ ] Создать новые таблицы в БД
- [ ] Реализовать endpoint'ы авторизации
- [ ] Реализовать endpoint'ы библиотеки
- [ ] Добавить middleware для проверки подписки
- [ ] Протестировать API (Postman/Insomnia)

### Этап 2: Frontend (4-5 дней)
- [ ] Настроить Next.js проект
- [ ] Реализовать страницу входа (Telegram Login Widget)
- [ ] Реализовать каталог материалов
- [ ] Реализовать страницу материала
- [ ] Реализовать поиск и фильтрацию
- [ ] Адаптивная вёрстка

### Этап 3: Админка (2-3 дня)
- [ ] Добавить команды в бота для управления материалами
- [ ] Реализовать загрузку обложек и файлов
- [ ] Реализовать статистику

### Этап 4: Тестирование (2 дня)
- [ ] Тестирование авторизации
- [ ] Тестирование проверки подписки
- [ ] Тестирование на разных устройствах
- [ ] Исправление багов

### Этап 5: Деплой (1 день)
- [ ] Настроить домен (library.momsclub.ru)
- [ ] Задеплоить backend на VPS
- [ ] Задеплоить frontend на Vercel
- [ ] Настроить SSL сертификат
- [ ] Финальное тестирование

**Итого: 12-15 дней**

---

## 8. СТОИМОСТЬ РАЗРАБОТКИ (примерная)

### 8.1 Разработка
- Backend: 40-50 часов × 2000₽ = **80 000 - 100 000₽**
- Frontend: 50-60 часов × 2000₽ = **100 000 - 120 000₽**
- Админка: 20-25 часов × 2000₽ = **40 000 - 50 000₽**
- Тестирование: 15-20 часов × 1500₽ = **22 500 - 30 000₽**

**Итого: 242 500 - 300 000₽**

### 8.2 Инфраструктура (ежемесячно)
- VPS сервер: уже есть (0₽)
- Домен library.momsclub.ru: ~500₽/год
- SSL сертификат: бесплатно (Let's Encrypt)
- Vercel hosting: бесплатно (до 100GB трафика)

**Итого: ~500₽/год**

---

## 9. АЛЬТЕРНАТИВНЫЕ РЕШЕНИЯ

### 9.1 MVP за 3-5 дней (упрощённая версия)
**Что убрать:**
- Админку (добавлять материалы вручную в БД)
- Аналитику (добавить позже)
- Сложные фильтры (только категории)

**Что оставить:**
- Авторизация через Telegram
- Каталог материалов
- Страница материала
- Проверка подписки

**Стоимость: 80 000 - 120 000₽**

### 9.2 Telegram Mini App (альтернатива сайту)
**Плюсы:**
- Всё внутри Telegram
- Не нужен домен
- Встроенная авторизация

**Минусы:**
- Ограничения по дизайну
- Сложнее разрабатывать
- Хуже UX на desktop

**Стоимость: 150 000 - 200 000₽**

---

## 10. КОНТАКТЫ И ДОСТУПЫ

### 10.1 Необходимые данные для разработки
- [ ] Telegram Bot Token
- [ ] Telegram Bot Username (для Login Widget)
- [ ] Доступ к серверу (SSH)
- [ ] Доступ к БД SQLite
- [ ] Домен для библиотеки (library.momsclub.ru)
- [ ] Дизайн-макеты (Figma) — опционально

### 10.2 Репозиторий
- GitHub: создать приватный репозиторий
- Структура:
  ```
  momsclub-library/
  ├── backend/       # FastAPI
  ├── frontend/      # Next.js
  ├── docs/          # Документация
  └── README.md
  ```

---

## 11. ПОДДЕРЖКА И РАЗВИТИЕ

### 11.1 После запуска
- Мониторинг ошибок (Sentry)
- Резервное копирование БД (ежедневно)
- Обновление контента (еженедельно)

### 11.2 Будущие фичи (v2.0)
- [ ] Избранное (сохранённые материалы)
- [ ] Комментарии к материалам
- [ ] Рекомендации (на основе просмотров)
- [ ] Уведомления о новых материалах
- [ ] Экспорт материалов в PDF
- [ ] Офлайн-режим (PWA)

---

## 12. КРИТЕРИИ ПРИЁМКИ

### 12.1 Функциональность
- ✅ Авторизация через Telegram работает
- ✅ Проверка подписки работает в реальном времени
- ✅ Каталог материалов отображается корректно
- ✅ Поиск и фильтрация работают
- ✅ Страница материала открывается и отображает контент
- ✅ При истечении подписки доступ закрывается
- ✅ Адаптивный дизайн (mobile, tablet, desktop)

### 12.2 Производительность
- ✅ Время загрузки главной страницы < 2 сек
- ✅ Время загрузки страницы материала < 1.5 сек
- ✅ Поиск работает без задержек

### 12.3 Безопасность
- ✅ JWT токены валидируются корректно
- ✅ Telegram данные проверяются на подлинность
- ✅ HTTPS работает на всех страницах
- ✅ Rate limiting настроен

---

## 13. КОНТАКТНАЯ ИНФОРМАЦИЯ

**Заказчик:** Всеволод (Mom's Club)  
**Telegram:** @your_username  
**Email:** support@momsclub.ru  

**Дата создания ТЗ:** 30 ноября 2025  
**Версия:** 1.0  

---

## ПРИЛОЖЕНИЯ

### Приложение А: Примеры материалов
1. "Как справиться с детскими истериками" (Воспитание детей)
2. "10 игр для развития мелкой моторики" (Развитие ребёнка)
3. "Здоровое питание для детей до 3 лет" (Здоровье и питание)
4. "Как найти баланс между работой и материнством" (Финансы и карьера)
5. "Техники расслабления для мам" (Саморазвитие)

### Приложение Б: Структура Markdown контента
```markdown
# Заголовок материала

## Введение
Краткое описание проблемы...

## Основная часть
### Подзаголовок 1
Текст...

![Изображение](https://...)

### Подзаголовок 2
Текст...

## Заключение
Выводы...

## Полезные ссылки
- [Ссылка 1](https://...)
- [Ссылка 2](https://...)
```

### Приложение В: API документация
После разработки будет доступна Swagger документация:
`https://api.momsclub.ru/docs`

---

**КОНЕЦ ТЕХНИЧЕСКОГО ЗАДАНИЯ**
